# Claude Desktop adapter

Example configuration for using Baldr Router as a generic MCP server from Claude Desktop.

Install the router. Install the optional launcher if you want auto-detection/Windows-to-WSL bridging, then copy the example into your Claude Desktop MCP configuration.
